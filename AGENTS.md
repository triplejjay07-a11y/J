# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently a blank project with only a `README.md`. No application code, dependencies, build system, or services exist yet.

- **No lint, test, build, or run commands** are available since no source code or configuration files exist.
- When source code and a dependency manifest (e.g. `package.json`, `requirements.txt`) are added, update the VM environment setup script accordingly.
- Future agents should re-evaluate the repository structure before attempting to run any commands.
